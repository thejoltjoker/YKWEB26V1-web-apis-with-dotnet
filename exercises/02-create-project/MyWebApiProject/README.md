# MyWebApi

MyWebApi uses listens on the following url when using `dotnet run`
- http://localhost:5112 

MyWebApi uses listens on the following url's when using `dotnet run --launch-profile https`
- https://localhost:7096
- http://localhost:5112